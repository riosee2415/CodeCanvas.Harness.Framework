# PRD: {프로젝트명}

## 목표
{이 프로젝트가 해결하려는 문제를 한 줄로 요약}

## 사용자
{누가 이 제품을 쓰는지}

## 핵심 기능
우선순위를 명시한다. P0 = MVP 필수, P1 = 있으면 좋음, P2 = 나중.

| 우선순위 | 기능 | 설명 |
|----------|------|------|
| P0 | {기능 1} | {한 줄 설명} |
| P0 | {기능 2} | {한 줄 설명} |
| P1 | {기능 3} | {한 줄 설명} |

## 사용자 시나리오
핵심 흐름을 사용자 관점에서 서술한다. 이게 곧 step AC의 근거가 된다.

1. {사용자가 ~하면 → 시스템이 ~한다 → 결과로 ~를 본다}
2. {시나리오 2}

## 성공 지표
"동작한다"의 객관적 정의. 측정 가능한 형태로 적는다.

- {예: 사용자가 3번의 클릭 이내에 핵심 작업을 완료할 수 있다}
- {예: 초기 로드 2초 이내}

## 엣지 케이스 / 에러 상황
정상 흐름 외에 반드시 처리해야 할 상황.

- {예: 입력이 비어 있을 때 → 검증 메시지 표시}
- {예: 외부 API 실패 시 → 재시도 없이 에러 상태 표시}
- {예: 빈 목록(empty state) → 안내 문구 표시}

## 비기능 요구사항
- 성능: {예: 인터랙션 응답 100ms 이내}
- 접근성: {예: 키보드만으로 모든 기능 사용 가능, 대비 WCAG AA}
- 지원 범위: {예: 최신 Chrome/Safari, 모바일 반응형}

## MVP 제외 사항
명시적으로 안 만들 것. step 설계 시 scope creep을 막는다.

- {안 만들 것 1}
- {안 만들 것 2}
- {안 만들 것 3}

## 디자인
- {디자인 방향 (예: 다크모드 고정, 미니멀)}
- {색상 (예: 무채색 + 포인트 1가지)}
